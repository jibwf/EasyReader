# GitHub Copilot Instructions for EasyReader

## Project Overview

**EasyReader** 是一个自托管的全栈阅读器项目（Python FastAPI + React），支持灵活的书源规则解析、多源搜索聚合、内容获取和TTS播放。

## Core Architecture

### Backend (Python FastAPI)

**模块边界**（不可跨越）：

| 模块 | 路径 | 职责 |
|------|------|------|
| 规则引擎 | `backend/engine/` | 书源规则解析（CSS/XPath/JSONPath/JS/Regex） |
| 数据模型 | `backend/models/` | SQLite ORM 定义 |
| 业务逻辑 | `backend/services/` | 搜索聚合、内容获取、书架管理 |
| API 路由 | `backend/routers/` | FastAPI 路由，薄层，不含业务逻辑 |
| TTS 引擎 | `backend/tts/` | 多后端 TTS（Edge/本地/自定义API） |
| 工具函数 | `backend/utils/` | 文本处理、图片代理等 |

**编码标准**：
- 类型注解必须（`from typing import ...`）
- async/await 优先（ FastAPI 原生支持）
- Pydantic 模型做数据验证
- 异常处理要明确（自定义异常类）

### Frontend (React + TypeScript)

| 模块 | 路径 | 职责 |
|------|------|------|
| 页面 | `frontend/src/pages/` | 页面级容器组件 |
| 组件 | `frontend/src/components/` | 可复用组件库 |
| 状态管理 | `frontend/src/stores/` | Zustand store |
| 工具函数 | `frontend/src/utils/` | API 调用、格式转换 |

**编码标准**：
- 严格 TypeScript 模式（`tsconfig.json` 中 `strict: true`）
- 函数式组件 + hooks（不用 class 组件）
- Zustand 做全局状态，组件 props 用 React Context
- 文件命名：kebab-case（文件），PascalCase（组件）

## Development Workflow

### Before Coding

1. **评估规模**：
   - 新模块 / 跨模块 / 改动 ≥3 文件 → 先出方案（审阅后再动手）
   - 单文件改动 / bug 修复 / 小调整 → 直接实现

2. **验证目标**：
   - 功能属于哪个模块？
   - 是否违反模块边界？
   - 需要修改哪些文件？

### Testing & Validation

**必须全绿**：
- `pytest tests/` （后端单元测试）
- `npm run build` （前端编译 + typecheck）
- `docker build .` （容器构建）

修改代码 → 自动验证 → 失败则自动修复 → 完成报告

### Documentation

- 中大功能方案保存到 `docs/`（Markdown 格式）
- 优先更新三份主文档：`docs/architecture.md`、`docs/phase1.md`、`e-link-client/docs/eink-client-plan.md`

## Coding Conventions

### Python (backend/)

```python
# 类型注解示例
from typing import Optional, List
from pydantic import BaseModel, Field

class BookSource(BaseModel):
    id: str = Field(..., description="源ID")
    name: str
    rules: Dict[str, str]  # CSS/XPath/JSONPath 规则

async def fetch_content(source_id: str) -> Optional[str]:
    """获取书源内容"""
    pass
```

### TypeScript (frontend/src/)

```typescript
// 组件命名：PascalCase
// 文件命名：kebab-case
export interface Book {
  id: string;
  title: string;
  coverUrl?: string;
}

export const BookCard: React.FC<{ book: Book }> = ({ book }) => {
  return <div>{book.title}</div>;
};
```

## Performance & Quality

1. **API 响应契约**：
   - 当前 API 直接返回 JSON 模型，不强制包 `code/message/data`。
   - 任何接口语义变更，先更新 `docs/architecture.md`。

2. **错误处理**：
   - 后端：自定义异常类 + FastAPI `HTTPException`
   - 前端：Zustand 中心化错误状态

3. **缓存策略**：
   - 后端：书源规则缓存、搜索结果缓存
   - 前端：IndexedDB（idb-keyval）缓存书籍列表

## Key Files to Know

- `backend/main.py` - FastAPI 应用入口
- `backend/database.py` - SQLite 连接管理
- `frontend/src/main.tsx` - React 应用入口
- `Dockerfile` - 单容器部署配置
- `pytest.ini` - 测试配置

## When Modifying Code

- ✅ 遵守模块边界
- ✅ 保持类型注解完整
- ✅ 同步更新相关测试
- ✅ 跑完整测试套件后再提交
- ❌ 不要跨模块乱改文件
- ❌ 不要引入多容器架构
- ❌ 不要忽略类型检查警告

## Quick Commands

```bash
# 后端开发
cd backend && pip install -r requirements-dev.txt
python -m uvicorn main:app --reload

# 前端开发
cd frontend && npm install && npm run dev

# 运行测试
pytest tests/

# Docker 构建
docker build -t reader:latest .
```

## Communication

- 英文代码注释
- 中文用户交互与文档
- 代码评审时指出模块归属问题
